# Scripts Directory

## Structure
- `training/` - Model training scripts
- `downstream/` - Downstream task evaluation scripts
- `utils/` - Utility and helper scripts
- `fixes/` - Debug and data preprocessing scripts

## Main Scripts (at root level)
- Keep actively used scripts here
